<Window x:Class="Royal_Blueberry_Dictionary.View.Dialogs.GameSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Game Settings"
        Width="600" Height="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MainBackground}">

    <Window.Resources>
        <!-- Dropdown Button Style -->
        <Style x:Key="DropdownButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SearchBackground}" />
            <Setter Property="Foreground" Value="{DynamicResource TextColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Padding" Value="15,10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource WordItemHover}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Popup Item Styles -->
        <Style x:Key="PopupItemStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextColor}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="15,10" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource WordItemHover}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PopupItemFirstStyle" TargetType="Button" BasedOn="{StaticResource PopupItemStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12,12,0,0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource WordItemHover}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PopupItemLastStyle" TargetType="Button" BasedOn="{StaticResource PopupItemStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="0,0,10,10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource WordItemHover}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Action Button Styles -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ToolButtonActive}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SearchBackground}" />
            <Setter Property="Foreground" Value="{DynamicResource ButtonColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource WordItemHover}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource CardBackground}"
            BorderBrush="{DynamicResource BorderColor}"
            BorderThickness="2"
            CornerRadius="20"
            Margin="20"
            Padding="35">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <TextBlock Text="⚙️ Game Settings"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextColor}"
                           Margin="0,0,0,8" />
                <TextBlock Text="Customize your learning experience"
                           FontSize="14"
                           Foreground="{DynamicResource TextColor}"
                           Opacity="0.7" />
            </StackPanel>

            <!-- Settings Content -->
            <StackPanel Grid.Row="1">
                <!-- Data Source Selection -->
                <TextBlock Text="📚 Vocabulary Source"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextColor}"
                           Margin="0,0,0,15" />

                <Grid Margin="0,0,0,10">
                    <Button x:Name="BtnDataSource"
                            Style="{StaticResource DropdownButtonStyle}"
                            Click="BtnDataSource_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="TxtSelectedSource" Text="📚 All Words" Grid.Column="0" />
                            <Path Grid.Column="1"
                                  Data="M 0 0 L 4 4 L 8 0"
                                  Stroke="{DynamicResource TextColor}"
                                  StrokeThickness="1.5"
                                  Margin="10,0,0,0"
                                  VerticalAlignment="Center" />
                        </Grid>
                    </Button>

                    <Popup x:Name="PopupDataSource"
                           PlacementTarget="{Binding ElementName=BtnDataSource}"
                           Placement="Bottom"
                           StaysOpen="False"
                           AllowsTransparency="True">
                        <Border Background="{DynamicResource SearchBackground}"
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="2"
                                CornerRadius="12"
                                Margin="0,2,0,0"
                                Padding="0"
                                ClipToBounds="True">
                            <StackPanel x:Name="DataSourceOptions" />
                        </Border>
                    </Popup>
                </Grid>

                <TextBlock x:Name="TxtAvailableCards"
                           Text="0 available words"
                           FontSize="12"
                           Foreground="{DynamicResource TextColor}"
                           Opacity="0.7"
                           Margin="0,0,0,25" />

                <!-- Card Count Selection -->
                <TextBlock Text="🎴 Card Count"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextColor}"
                           Margin="0,0,0,15" />

                <Grid Margin="0,0,0,25">
                    <Button x:Name="BtnCardCount"
                            Style="{StaticResource DropdownButtonStyle}"
                            Click="BtnCardCount_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="TxtSelectedCount" Text="10 cards" Grid.Column="0" />
                            <Path Grid.Column="1"
                                  Data="M 0 0 L 4 4 L 8 0"
                                  Stroke="{DynamicResource TextColor}"
                                  StrokeThickness="1.5"
                                  Margin="10,0,0,0"
                                  VerticalAlignment="Center" />
                        </Grid>
                    </Button>

                    <Popup x:Name="PopupCardCount"
                           PlacementTarget="{Binding ElementName=BtnCardCount}"
                           Placement="Bottom"
                           StaysOpen="False"
                           AllowsTransparency="True">
                        <Border Background="{DynamicResource SearchBackground}"
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="2"
                                CornerRadius="12"
                                Margin="0,2,0,0"
                                Padding="0"
                                ClipToBounds="True">
                            <StackPanel>
                                <Button Content="5 cards" Click="CardCountOption_Click" Tag="5"
                                        Style="{StaticResource PopupItemFirstStyle}" />
                                <Button Content="10 cards" Click="CardCountOption_Click" Tag="10"
                                        Style="{StaticResource PopupItemStyle}" />
                                <Button Content="15 cards" Click="CardCountOption_Click" Tag="15"
                                        Style="{StaticResource PopupItemStyle}" />
                                <Button Content="20 cards" Click="CardCountOption_Click" Tag="20"
                                        Style="{StaticResource PopupItemStyle}" />
                                <Button Content="30 cards" Click="CardCountOption_Click" Tag="30"
                                        Style="{StaticResource PopupItemLastStyle}" />
                            </StackPanel>
                        </Border>
                    </Popup>
                </Grid>
            </StackPanel>

            <!-- Action Buttons -->
            <UniformGrid Grid.Row="2" Columns="2" Rows="1">
                <Button Content="◀ Cancel"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,7,0"
                        Click="Cancel_Click" />
                <Button Content="▶ Start Playing"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="7,0,0,0"
                        Click="StartGame_Click" />
            </UniformGrid>
        </Grid>
    </Border>
</Window>