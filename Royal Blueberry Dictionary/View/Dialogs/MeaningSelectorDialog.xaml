<Window x:Class="Royal_Blueberry_Dictionary.View.Dialogs.MeaningSelectorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:Royal_Blueberry_Dictionary.Converters"
        mc:Ignorable="d"
        Title="Save word to My Words"
        Height="700" Width="860"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource MainBackground}">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <conv:StringNullOrEmptyToVisibilityConverter x:Key="StringNullToVisibility"/>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource BorderColor}"
            BorderThickness="2"
            CornerRadius="20"
            Margin="20"
            Padding="30"
            Background="{DynamicResource CardBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="📚 Save word to My Words"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextColor}"
                           Margin="0,0,0,10"/>

                <TextBlock x:Name="WordTitleText"
                           Text="{Binding Detail.Word, FallbackValue=Word}"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="{DynamicResource ButtonColor}"/>

                <TextBlock x:Name="StepIndicator"
                           Text="Step 1/2: Select meaning"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Opacity="0.8"
                           Foreground="{DynamicResource ButtonColor}"
                           Margin="0,10,0,0"/>
            </StackPanel>

            <!-- CONTENT -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          Margin="0,0,0,20">
                <Grid>
                    <!-- STEP 1: MEANINGS -->
                    <StackPanel x:Name="MeaningsPanel">
                        <TextBlock Text="Click on the meaning you want to save:"
                                   FontSize="14"
                                   Opacity="0.8"
                                   Foreground="{DynamicResource TextColor}"
                                   Margin="0,0,0,15"/>

                        <ListBox x:Name="MeaningsList"
                                 ItemsSource="{Binding Detail.Meanings}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 SelectionChanged="MeaningsList_SelectionChanged">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Margin" Value="0,0,0,12"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="Card"
                                                        Background="{DynamicResource WordItemBackground}"
                                                        BorderBrush="{DynamicResource BorderColor}"
                                                        BorderThickness="2"
                                                        CornerRadius="14"
                                                        Padding="18">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Card" Property="Background" Value="{DynamicResource WordItemHover}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource ToolButtonActive}"/>
                                                        <Setter TargetName="Card" Property="BorderThickness" Value="3"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <Border Background="{DynamicResource ToolButtonActive}"
                                                    CornerRadius="14"
                                                    Padding="10,4"
                                                    Margin="0,0,10,0">
                                                <TextBlock Text="{Binding PartOfSpeech}"
                                                           Foreground="White"
                                                           FontWeight="Bold"/>
                                            </Border>
                                            <Border Background="{DynamicResource SearchBackground}"
                                                    BorderBrush="{DynamicResource BorderColor}"
                                                    BorderThickness="1"
                                                    CornerRadius="10"
                                                    Padding="8,4">
                                                <TextBlock Text="{Binding MeaningIndex}"
                                                           FontWeight="SemiBold"
                                                           Foreground="{DynamicResource TextColor}"/>
                                            </Border>
                                        </StackPanel>

                                        <ItemsControl ItemsSource="{Binding Definitions}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Margin="0,0,0,12">
                                                        <TextBlock TextWrapping="Wrap" FontSize="15">
                                                            <Run Text="• " FontWeight="Bold" Foreground="{DynamicResource ButtonColor}"/>
                                                            <Run Text="{Binding Text}"/>
                                                        </TextBlock>
                                                        <Border Background="{DynamicResource ExampleBackground}"
                                                                BorderBrush="{DynamicResource ExampleBorder}"
                                                                BorderThickness="3,0,0,0"
                                                                CornerRadius="6"
                                                                Padding="10"
                                                                Margin="25,8,0,0"
                                                                Visibility="{Binding Example, Converter={StaticResource StringNullToVisibility}}">
                                                            <TextBlock Text="{Binding Example}"
                                                                       FontStyle="Italic"
                                                                       Opacity="0.8"
                                                                       TextWrapping="Wrap"/>
                                                        </Border>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>

                    <!-- STEP 2: TAGS -->
                    <StackPanel x:Name="TagsPanel" Visibility="Collapsed">
                        <!-- Selected Meaning Info -->
                        <Border Background="{DynamicResource WordItemBackground}"
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="2"
                                CornerRadius="12"
                                Padding="15"
                                Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="✅" FontSize="20" Margin="0,0,10,0"/>
                                <TextBlock x:Name="SelectedMeaningText"
                                           Text="Select a meaning first"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextColor}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- Tags Selection -->
                        <TextBlock Text="Select a tag for this word (optional):"
                                   FontSize="14"
                                   Opacity="0.8"
                                   Foreground="{DynamicResource TextColor}"
                                   Margin="0,0,0,15"/>

                        <ListBox x:Name="TagsList"
                                 ItemsSource="{Binding Tags}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 SelectionChanged="TagsList_SelectionChanged">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>

                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0,0,10,10"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="TagCard"
                                                        Background="{DynamicResource SearchBackground}"
                                                        BorderBrush="{DynamicResource BorderColor}"
                                                        BorderThickness="2"
                                                        CornerRadius="10"
                                                        Padding="12,10"
                                                        Cursor="Hand">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="TagCard" Property="Background" Value="{DynamicResource WordItemHover}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="TagCard" Property="BorderBrush" Value="{DynamicResource ToolButtonActive}"/>
                                                        <Setter TargetName="TagCard" Property="BorderThickness" Value="3"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Width="10" Height="10" CornerRadius="5"
                                                Background="{Binding Color}"
                                                Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Icon}"
                                                   FontSize="16"
                                                   Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextColor}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <!-- FOOTER -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <!-- Back Button (only in step 2) -->
                <Button x:Name="BackButton"
                        Content="⬅️ Go Back"
                        Padding="20,12"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Margin="0,0,10,0"
                        Click="BackButton_Click"
                        Visibility="{Binding ElementName=TagsPanel, Path=Visibility}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{DynamicResource SearchBackground}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="12"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource WordItemHover}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Skip Tags Button (only in step 2) -->
                <Button Content="⏭️ Skip"
                        Padding="20,12"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Margin="0,0,10,0"
                        Click="SkipTagsButton_Click"
                        Visibility="{Binding ElementName=TagsPanel, Path=Visibility}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{DynamicResource SearchBackground}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="12"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource WordItemHover}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Cancel Button -->
                <Button Content="❌ Cancel"
                        Padding="20,12"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Margin="0,0,10,0"
                        Click="CancelButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{DynamicResource SearchBackground}"/>
                            <Setter Property="Foreground" Value="#DC2626"/>
                            <Setter Property="BorderBrush" Value="#DC2626"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="12"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#DC2626"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Save Button (only in step 2) -->
                <Button Content="💾 Save Word"
                        Padding="20,12"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Click="SaveButton_Click"
                        Visibility="{Binding ElementName=TagsPanel, Path=Visibility}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{DynamicResource ToolButtonActive}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="12"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
