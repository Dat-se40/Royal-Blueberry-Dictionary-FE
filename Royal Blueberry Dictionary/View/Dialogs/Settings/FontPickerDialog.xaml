<Window x:Class="Royal_Blueberry_Dictionary.View.Dialogs.Settings.FontPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose Font"
        Width="640"
        Height="720"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernFontSizeCombo" TargetType="ComboBox" BasedOn="{StaticResource SettingsComboBoxStyle}">
            <Setter Property="Height" Value="48"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>

        <!-- Font List Item Style -->
        <Style x:Key="FontListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="20,16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="0">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0" Opacity="0.1">
                                <GradientStop Color="#667EEA" Offset="0"/>
                                <GradientStop Color="#764BA2" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0" Opacity="0.15">
                                <GradientStop Color="#667EEA" Offset="0"/>
                                <GradientStop Color="#764BA2" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Foreground" Value="{DynamicResource ButtonColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource CardBackground}"
            BorderBrush="{DynamicResource BorderColor}"
            BorderThickness="2"
            CornerRadius="24">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.3" Color="Black"/>
        </Border.Effect>

        <Grid Margin="36">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,28">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#20667EEA"
                            Width="56" Height="56"
                            CornerRadius="16"
                            Margin="0,0,16,0">
                        <TextBlock Text="🔤"
                                   FontSize="32"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Font &amp; Size"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TextColor}"/>
                        <TextBlock Text="Customize your reading experience"
                                   FontSize="13"
                                   Foreground="{DynamicResource TextColor}"
                                   Opacity="0.7"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>

                <Button Content="✕"
                        Width="48" Height="48"
                        FontSize="24"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{DynamicResource TextColor}"
                        Cursor="Hand"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Click="Close_Click"/>
            </Grid>

            <!-- Font Size Selector -->
            <Border Grid.Row="1"
                    Background="#10667EEA"
                    BorderBrush="#30667EEA"
                    BorderThickness="1"
                    CornerRadius="16"
                    Padding="20,16"
                    Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Font Size:"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextColor}"
                               VerticalAlignment="Center"
                               Margin="0,0,20,0"/>

                    <ComboBox Grid.Column="1"
                              x:Name="FontSizeComboBox"
                              SelectedIndex="2"
                              Style="{StaticResource ModernFontSizeCombo}"
                              SelectionChanged="FontSizeComboBox_SelectionChanged">
                        <ComboBoxItem Content="Small (12pt)" Tag="12" Style="{StaticResource ComboBoxItemFirstStyle}"/>
                        <ComboBoxItem Content="Normal (14pt)" Tag="14" Style="{StaticResource ComboBoxItemStyle}"/>
                        <ComboBoxItem Content="Medium (16pt)" Tag="16" Style="{StaticResource ComboBoxItemStyle}"/>
                        <ComboBoxItem Content="Large (18pt)" Tag="18" Style="{StaticResource ComboBoxItemStyle}"/>
                        <ComboBoxItem Content="Extra Large (20pt)" Tag="20" Style="{StaticResource ComboBoxItemLastStyle}"/>
                    </ComboBox>
                </Grid>
            </Border>

            <!-- Font List -->
            <Border Grid.Row="2"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="2"
                    CornerRadius="16"
                    Background="{DynamicResource SearchBackground}">
                <ListBox x:Name="FontListBox"
                         BorderThickness="0"
                         Background="Transparent"
                         SelectionMode="Single"
                         SelectionChanged="FontListBox_SelectionChanged"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         ItemContainerStyle="{StaticResource FontListItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0"
                                           Text="{Binding Source}"
                                           FontFamily="{Binding}"
                                           FontSize="17"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextColor}"/>

                                <TextBlock Grid.Row="1"
                                           Text="The quick brown fox jumps over the lazy dog"
                                           FontFamily="{Binding}"
                                           FontSize="13"
                                           Foreground="{DynamicResource TextColor}"
                                           Opacity="0.65"
                                           Margin="0,6,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Preview -->
            <Border Grid.Row="3"
                    Background="{DynamicResource CardBackground}"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="2"
                    CornerRadius="16"
                    Padding="24,20"
                    Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="📖 Preview"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextColor}"
                               Opacity="0.7"
                               Margin="0,0,0,12"/>
                    <TextBlock x:Name="PreviewText"
                               Text="Royal Blueberry Dictionary – Your English Learning Companion"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="{DynamicResource TextColor}"
                               TextWrapping="Wrap"
                               LineHeight="26"/>
                </StackPanel>
            </Border>

            <!-- Apply Button -->
            <Button Grid.Row="4"
                    Content="✓ Apply Font"
                    Padding="0,16"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Margin="0,20,0,0"
                    Cursor="Hand"
                    Click="Apply_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background">
                            <Setter.Value>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#667EEA" Offset="0"/>
                                    <GradientStop Color="#764BA2" Offset="1"/>
                                </LinearGradientBrush>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="14"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#5568D3" Offset="0"/>
                                            <GradientStop Color="#6A42A0" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>